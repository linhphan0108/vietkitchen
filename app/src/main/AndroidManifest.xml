<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.linh.vietkitchen">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".ui.VietKitchenApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.screen.splashScreen.SplashScreenActivity"
                  android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.screen.login.LoginActivity"/>
        <activity android:name=".ui.screen.home.homeActivity.HomeActivity"
                  android:label="@string/app_name">
        </activity>

        <activity android:name=".ui.screen.detailActivity.RecipeDetailActivity"
                  android:theme="@style/TranslucentStatusBar"
                  android:launchMode="singleTop"
                  android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts URIs that begin with "http://www.example.com/gizmos” -->
                <!--note that the leading "/" is required for pathPrefix-->
                <data android:scheme="http"
                      android:host="www.example.com"
                      android:pathPrefix="/gizmos" />
                <!--Accepts URIs that begin with "example://gizmos”-->
                <data android:scheme="example"
                      android:host="gizmos" />
            </intent-filter>
        </activity>

        <activity android:name=".admin.ui.screen.admin.AdminActivity"
                  android:theme="@style/TranslucentStatusBar"/>

        <activity android:name=".ui.screen.searchScreen.SearchScreenActivity"
                  android:label="@string/app_name"
                  android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>

        <service android:name=".ui.service.PutRecipeService"/>
        <service android:name=".ui.service.ReStructureImageFileService"/>
    </application>

</manifest>